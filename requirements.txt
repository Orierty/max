# Основные зависимости (нужны всегда)
requests
python-dotenv
psycopg2-binary
gigachat
Pillow


# AI библиотеки (раскомментируй если нужны)

# Для Vision Model (GigaChat Pro) - если VISION_MODEL_ENABLED=true
gigachat       # API для работы с GigaChat
pillow         # Конвертация изображений в JPEG

# Для Voice Control (голосовое управление) - если VOICE_ENABLED=true
vosk          # Распознавание речи офлайн
pydub         # Конвертация аудио форматов

# ВАЖНО! Для работы pydub нужен ffmpeg в системе:
# Windows: winget install ffmpeg  ИЛИ  choco install ffmpeg
# Linux: sudo apt install ffmpeg
# Mac: brew install ffmpeg
