# Max.ru Бот волонтёров

Бот для платформы Max.ru, который соединяет волонтёров с нуждающимися в помощи людьми.

## Возможности

### Для нуждающихся
- 📞 **Запрос звонка от волонтёра** - система волновых уведомлений (15 волонтёров за раз)
- 🎤 **Голосовое → Текст** - преобразование голосовых сообщений в текст
- 📷 **Изображение → Текст** - AI распознавание изображений (опционально)
- 👁️ **Описание фото волонтёром** - человек описывает фото текстом
- 🔙 **Удобная навигация** - кнопка "Назад в меню" на всех экранах

### Для волонтёров
- ✅ **Система верификации** - три уровня доступа (новичок, верифицированный, доверенный)
- 📊 **Статистика** - рейтинг, количество звонков, отзывы
- 🏷️ **Теги пользователей** - бабушка/дедушка, незрячий, плохая камера и т.д.
- 👁️ **Описание фото** - помощь в описании изображений для незрячих
- ⭐ **Рейтинговая система** - оценки от 1 до 5 звёзд

### Для модераторов
- 🛡️ **Панель модератора** - управление верификацией и жалобами
- 🚫 **Блокировка волонтёров** - с указанием причины
- ✅ **Верификация** - одобрение/отклонение заявок волонтёров

## Технологии

- **Backend**: Python 3.8+
- **База данных**: PostgreSQL (connection pooling)
- **AI модель**: Qwen2-VL-2B (опционально, для распознавания изображений)
- **Голосовые команды**: Vosk (офлайн распознавание речи)
- **Мессенджер**: Max.ru API

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd max
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка PostgreSQL

Запустите PostgreSQL через Docker:

```bash
docker compose up -d
```

Или настройте вручную:
```sql
CREATE DATABASE max_bot;
```

База данных автоматически создаст все необходимые таблицы при первом запуске.

### 4. Конфигурация

Скопируйте `.env.example` в `.env` и заполните свои данные:

```env
MAX_TOKEN=ваш_токен_от_max_ru
VISION_MODEL_ENABLED=false              # true для AI распознавания изображений
VOICE_ENABLED=false                     # true для голосового управления
DB_HOST=localhost
DB_PORT=5432
DB_NAME=max_bot
DB_USER=postgres
DB_PASSWORD=ваш_пароль
```

### 5. Запуск

```bash
python main.py
```

## Опциональные возможности

### AI распознавание изображений

Если хотите включить AI распознавание изображений:

1. Установите дополнительные зависимости (уже в requirements.txt)
2. Установите `VISION_MODEL_ENABLED=true` в `.env`
3. При первом запуске модель Qwen2-VL-2B автоматически скачается (~5GB)

### Голосовое управление

Для включения голосового управления:

1. Установите зависимости:
```bash
pip install vosk pydub
```

2. Скачайте модель для русского языка:
   - https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip (~45MB)
   - Распакуйте в `models/vosk-model-small-ru-0.22/`

3. Установите ffmpeg:
   - **Windows**: `winget install ffmpeg` или `choco install ffmpeg`
   - **macOS**: `brew install ffmpeg`
   - **Linux**: `sudo apt install ffmpeg`

4. Установите `VOICE_ENABLED=true` в `.env`

**Поддерживаемые голосовые команды:**
- "Позвоните мне волонтёр"
- "Покажи меню"
- "Опиши картинку"

## Структура проекта

```
max/
├── bot/                      # Код бота
│   ├── config.py            # Конфигурация
│   ├── handlers/            # Обработчики
│   │   ├── messages.py      # Текстовые сообщения
│   │   ├── callbacks.py     # Callback-кнопки
│   │   ├── menu.py          # Меню
│   │   ├── requests.py      # Запросы на помощь
│   │   ├── image.py         # Обработка изображений
│   │   ├── voice.py         # Голосовые сообщения
│   │   ├── verification.py  # Верификация и описание фото
│   │   └── moderator.py     # Панель модератора
│   └── utils/               # Утилиты
│       ├── max_api.py       # Max.ru API
│       ├── vision.py        # AI модель
│       ├── voice.py         # Vosk распознавание
│       ├── messaging.py     # Обёртки для сообщений
│       └── debounce.py      # Защита от множественных нажатий
├── database.py              # PostgreSQL (connection pool)
├── db/
│   └── schema.sql           # SQL схема
├── main.py                  # Точка входа
├── requirements.txt         # Зависимости
├── .env.example             # Пример конфигурации
├── docker-compose.yml       # Docker для PostgreSQL
└── downloads/               # Временные файлы
```

## Команды бота

- `/start` - Начать работу с ботом (регистрация)
- `/menu` - Показать меню функций
- `/switch` - Переключить роль (волонтёр ⇄ нуждающийся)

## Особенности реализации

### Волновая система уведомлений
- Запросы отправляются волнами по 15 волонтёров
- Автоматическая отправка следующей волны при отсутствии ответа
- Исключение волонтёров из предыдущих волн

### Защита от множественных нажатий (Debounce)
- Критичные действия имеют таймаут 2 секунды
- Предотвращает дублирование запросов

### Connection Pooling для PostgreSQL
- Пул подключений (5-20 соединений)
- Автоматическое переподключение при потере связи
- Оптимизация производительности

### Система рейтингов и отзывов
- Оценка волонтёров от 1 до 5 звёзд
- Текстовые отзывы
- Подсчёт среднего рейтинга

### Теги пользователей
- Автоматическая пометка особенностей пользователей
- Помогает волонтёрам подготовиться к общению

## Разработка

### Логирование

Логи сохраняются в `bot.log` с уровнем DEBUG.

### База данных

Схема базы данных находится в `db/schema.sql` и включает:
- users - пользователи
- volunteers - информация о волонтёрах
- requests - запросы на помощь
- reviews - отзывы
- verification_requests - заявки на верификацию
- photo_description_requests - запросы на описание фото
- complaints - жалобы
- action_logs - логи действий

## Лицензия

Этот проект создан для помощи людям через платформу Max.ru.
